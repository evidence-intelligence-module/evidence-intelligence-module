# Open Queries — Satellite Evidence Parity Roadmap

Questions that genuinely have no answer in `documents/` (including `documents/research/satellite-parity-global-precedent-research.md`) and are deliberately deferred rather than guessed at.

**Status vocabulary** (same in `specs/001-evidence-generation-pipeline/issue/`):

- **Open** — no answer yet. Blocks whatever the last column names.
- **Narrowed** — part of the question turned out to have an unambiguous right answer and was fixed; the part needing a decision is still live.
- **Split** — the file bundled a defect with a decision. The defect moved to `tasks.md`/`data-model.md`; the decision stays here.
- **Provisional default** — a default was adopted so work could proceed, chosen because it required no unsourced figure. Reversible, and the residual question is named in the file. Not the same as Resolved.
- **Resolved** — decided and recorded. Kept in this directory for the audit trail, not because it's still live.

Every filename starts `open query - ` because that was the convention when the tracker was created. It is a naming prefix, not a status — read the Status column, not the filename. Several files are linked by path from `spec.md`, `plan.md`, and `tasks.md`, so they aren't renamed when their status changes.

A second pass on 2026-08-13 re-assessed every entry against the code rather than against what it said when filed. Three had a default available that needed no unsourced figure and were closed provisionally; two turned out to contain a defect rather than only a decision, and those halves moved to [`../tasks.md`](../tasks.md) and [`../data-model.md`](../data-model.md). **Provisional default** below means adopted and reversible — recorded rather than assumed, the same treatment the commercial-tasking entry got.

| Status | File | Surfaced during | Blocks / what it gates |
|---|---|---|---|
| **Open** | [`open query - what the parity claim is validated against (SC-002, US3).md`](./open%20query%20-%20what%20the%20parity%20claim%20is%20validated%20against%20%28SC-002%2C%20US3%29.md) | scope re-evaluation | `spec.md` User Story 3, SC-001/SC-002, `tasks.md` T020–T025. Mirror of [`001`'s AI/ML label query](../../001-evidence-generation-pipeline/issue/open%20query%20-%20AI-ML%20training%20data%20source%20and%20CCE-label%20question.md), which is **authoritative and is the root of both trackers** — resolve there |
| **Narrowed** ×2 2026-08-13 | [`open query - SAR damage semantics for non-flood perils (FR-001).md`](./open%20query%20-%20SAR%20damage%20semantics%20for%20non-flood%20perils%20%28FR-001%29.md) | scope re-evaluation | `spec.md` User Story 1, FR-001, `tasks.md` T010. Polarization half fixed as a defect (T0-15); magnitude-calibration half moved to `001`'s label query. **Live question: whether SAR is reached for non-flood perils** — decidable now, and separating it means US1 no longer waits on the label decision |
| **Split** 2026-08-13 | [`open query - supplementary evidence re-evaluation and package supersession (FR-006).md`](./open%20query%20-%20supplementary%20evidence%20re-evaluation%20and%20package%20supersession%20%28FR-006%29.md) | scope re-evaluation | Package lineage was a defect → `tasks.md` T0-16 + `data-model.md`. **Live question: whether attaching evidence re-evaluates anything** — today the low-tier guidance promises an effect nothing delivers |
| **Provisional default** 2026-08-13 | [`open query - confidence tier threshold values (FR-004).md`](./open%20query%20-%20confidence%20tier%20threshold%20values%20%28FR-004%29.md) | scope re-evaluation | No longer blocks `T016`. Tier assigned by a rule table over the evidence-inputs manifest (`T0-09`), no unsourced cut point. Open: whether to add a numeric component once `T0-05` and Pilot data exist. FR-004's wording needs reconciling |
| **Provisional default** 2026-08-13 | [`open query - crop cross-check accuracy floor and discrepancy-flag harm posture (FR-010).md`](./open%20query%20-%20crop%20cross-check%20accuracy%20floor%20and%20discrepancy-flag%20harm%20posture%20%28FR-010%29.md) | scope re-evaluation | Three-state `outcome` + mandatory framing adopted → `data-model.md`. Open: the accuracy floor, and whether to ship User Story 4 at all — still the only capability whose failure mode harms an identifiable individual |
| **Provisional default** 2026-08-13 | [`open query - personal data in caller-supplied attachment metadata (FR-006).md`](./open%20query%20-%20personal%20data%20in%20caller-supplied%20attachment%20metadata%20%28FR-006%29.md) | scope re-evaluation | No longer blocks `T018`. `caller_supplied_metadata` dropped (nothing read it); `uri` constrained and non-dereference asserted. Open: reconciling Constitution §7's retention floor with DPDP obligations — a `documents/` change, larger than this endpoint |
| **Resolved** 2026-08-12 | [`open query - commercial satellite tasking budget and volume thresholds.md`](./open%20query%20-%20commercial%20satellite%20tasking%20budget%20and%20volume%20thresholds.md) | `/speckit-plan` | Nothing — decided free-only (Option A). `plan.md` Scale/Scope and `research.md` §5 are written against it; `commercial_tasking_client.py` ships as a disabled stub (T037) |

The six entries marked "scope re-evaluation" were surfaced on 2026-08-13 by a full re-reading of this feature's artifacts against the running code in `src/`, rather than during `/speckit-specify` or `/speckit-plan`. That pass also found defects with unambiguous right answers — those are not open queries and are tracked as Phase 0 tasks in [`../tasks.md`](../tasks.md), not here.

None of these were invented — this file documents what was checked and why no sourced answer exists, per this repo's convention against unsourced figures (`CLAUDE.md` Working conventions), the same discipline `specs/001-evidence-generation-pipeline/issue/` follows.
